# Required. Already present in the repo-root .env — do not duplicate it here.
OPENAI_API_KEY=
# Optional: use Tavily instead of OpenAI's built-in web search (set web.search_provider: tavily in config.yaml)
TAVILY_API_KEY=
# Optional: pause at every gate for a human decision (LangGraph interrupt)
HUMAN_APPROVAL=false
